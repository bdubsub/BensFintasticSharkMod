name: ci

on:
  pull_request:
  push:
    branches: [1.20.1, envy/0.24]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
      - uses: gradle/actions/wrapper-validation@v4
      - run: python3 -B -m unittest discover -s tools/performance -p 'test_*.py' -v
      - run: chmod +x ./gradlew
      - run: ./gradlew :forge:build --no-daemon
