# Verification scope amendment

Source role: owner request. Decision: DEC-012.

The owner requested, “you can forget the performance aspect of it. And the laptop aspect. Just ignore it and continue”.

This changes the remaining verification scope. Performance investigation, benchmark comparisons, measured diagnostic overhead and all laptop client acceptance are deferred from the testing JAR endpoint. Existing gameplay fixes, bounded diagnostics and real server tests remain required, together with package, merge, default verification, signed tag, documentation, delivery and cleanup gates. Historical failed or incomplete measurements are preserved as recorded; deferral is not a passing result. The saved goal and phase cursor are unchanged.
