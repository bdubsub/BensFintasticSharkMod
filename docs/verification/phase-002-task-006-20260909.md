# Phase 002 Task 006 Tiger Timing Baseline

Date: September 9, 2026

The Tiger bite damage delay is now bound to the authored jaw peak in `animation.tigershark.bite`. The peak occurs at 0.25 seconds, or five server ticks. The Forge death trigger is also edge triggered at the first death tick so it cannot restart the death presentation every tick.

The focused Forge suite passed:

```text
./gradlew :forge:test --no-daemon --console=plain
BUILD SUCCESSFUL in 11s

30 tests completed, 0 failures
```

This is a Task 006 diagnostic baseline only. Tiger target acquisition, three dimensional pursuit, physical contact, single damage windows, curiosity precedence, path failure, target invalidation, recovery, multiplayer behavior, and visual approval remain open.
