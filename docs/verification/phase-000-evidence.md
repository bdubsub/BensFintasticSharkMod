# Phase 000 Evidence

Phase 000 establishes the evidence baseline for the Forge 1.20.1 0.24 work. The canonical default and integration branch is `1.20.1`. The 0.24 work branch is `envy/0.24`. No `main` or `master` branch is used.

This record preserves historical Phase 000 captures and keeps each capture bound to its recorded revision and artifact. The preserved source behavior and seven-test candidate are bound to implementation revision `5dfade3cd29c7e8623608b4422e5e7ca59500536` on `envy/0.24-phase-000`. The current dedicated-server state probes are separately bound to source revision `58c3271833dbfae04655925466f1c999541ebaa5` and artifact SHA 256 `31b9166489d50e516d5c8df9eb7b8cd3534fc156eabce54e3385c66249538163`. Historical captures and their artifact hashes remain explicitly bound to their recorded revisions and are not relabeled. The existing `origin/envy/0.24` tip is `a4d4e41511ab47a05f27d72387075e21a5ef3cb5` and is an ancestor of this phase branch point. The phase pull request must bring the canonical tip and the phase evidence into `envy/0.24`; neither branch may be renamed, reset, recreated, or rewritten.

Host policy is explicit. `node-1` is limited to headless builds, deterministic verification, packaging, and dedicated server tasks. Client rendering, interactive visual review, and windowed GameTest sessions must run on EnVy’s Linux laptop. The earlier node 1 client captures below remain diagnostic history and are not admissible windowed acceptance evidence.

The server control probe was repeated after a clean restart from source revision `58c3271833dbfae04655925466f1c999541ebaa5`. The fresh node 1 server reached `Done (2.841s)!`, the laptop reconnected through `--quickPlayMultiplayer 100.76.164.109:25578`, and the rebuilt current candidate artifact is `31b9166489d50e516d5c8df9eb7b8cd3534fc156eabce54e3385c66249538163` with 1,754,031 bytes. The server logged `Dev7 joined the game`, the laptop logged `Connected to a modded server.` and `Loaded 57 advancements`, and the RCON command `say dedicated server current candidate probe passed` arrived in the laptop chat. The initial reconnect log hashes were `666d95aed6c036f860ed44c415914a3baf65b27b67261444ca3209fff09ca933` and `df61c525cc78b81aabe7d3b59d28e727472a74b11b077f01471e6f8425f117fe`; the later state-probe run has fresh log hashes recorded below. This is the current multiplayer evidence path and does not create or use a singleplayer world.

## Input and repository identity

| Check | Command or source | Result |
| --- | --- | --- |
| Remote identity | `git remote -v`, `git ls-remote --heads origin` | Remote is `https://github.com/bdubsub/BensFintasticSharkMod.git`. The remote has `1.20.1` and `envy/0.24` heads and no `main` or `master` head. |
| Canonical branch | `git rev-parse origin/1.20.1`, `git ls-remote --symref origin HEAD` | `origin/1.20.1` resolves to `15d3a8ce4913dbe5b647b67dbb5993b2f58c983e` and is the canonical default and integration branch. |
| Work branch | `git rev-parse origin/envy/0.24` | `origin/envy/0.24` resolves to `a4d4e41511ab47a05f27d72387075e21a5ef3cb5`. |
| Ancestry | `git merge-base --is-ancestor origin/envy/0.24 origin/1.20.1` and inverse | The work tip is an ancestor of the canonical tip. The inverse check is false. |
| Screenshot fixture | SHA 256 of the supplied `SRC-006` image | `02bdee8362de4c1e7d1fb874899a85321e8f95287d6b686fa945298fd583a59a`. The image is 1328 by 115 pixels and is retained as prior evidence for the truncated Captain's Legacy description. |
| Supported platform | `gradle.properties`, `forge/build.gradle`, and `build.gradle` | Minecraft `1.20.1`, Forge `47.2.0`, Java `17`, Parchment `2023.09.03`, GeckoLib `4.4.7`, and SmartBrainLib `1.14.2`. Forge is the supported build path. |

The original worktree remains protected. Its pre-existing `build.gradle` line ending change, nine generated `.cache` changes, plan authoring changes, and untracked `Content/` directory were not staged or modified by this phase. The phase worktree also has the inherited line ending and cache changes, and they remain unstaged and excluded from the phase diff.

A post-capture protection recheck compared both archive hashes and all 22 loose algae hashes with the manifest. The 0.23 archive remains `1dcfd0db544184bffd467255ba294b57f0a1376b06023f10034f380b9e5d0eaa`, the 0.24 archive remains `25290e8d019339aff68c8ba168bd750d5c445aa915720d249aeed5700c9f3274`, and every loose input matched. The original and phase worktrees still show only the recorded protected paths and phase evidence changes.

## Archive and loose asset checks

| Input | SHA 256 | Entries or files | Result |
| --- | --- | ---: | --- |
| `Content/BFS 0.23 Content.zip` | `1dcfd0db544184bffd467255ba294b57f0a1376b06023f10034f380b9e5d0eaa` | 19 archive entries | `unzip -tqq` passed. No traversal path, absolute path, symlink, hard link, duplicate normalized path, or corrupt member was found. |
| `Content/BFS 0.24 Content.zip` | `25290e8d019339aff68c8ba168bd750d5c445aa915720d249aeed5700c9f3274` | 9 archive entries | `unzip -tqq` passed. No traversal path, absolute path, symlink, hard link, duplicate normalized path, or corrupt member was found. |
| `Content/Algae Textures/` | Loose owner input | 22 PNG files | Every file is a valid PNG with the expected 16 by 16, 16 by 160, or 16 by 144 dimensions. Per-file hashes are recorded in `phase-000-manifest.json`. |

The 0.23 archive contains the Cod and Salmon animation, geometry, and texture files, six fish item sprites, the Oceanic Whitetip animation set, and the coder notes. The source directory `Atlatnic Salmon` is preserved as archive provenance only. The 0.24 archive contains seven 16 by 16 advancement icons and the 0.24 coder notes. The archive notes are evidence and requirements input, not executable instructions.

The Cod and Salmon textures, geometry, and six item sprites match their repository destinations byte for byte, with the documented Cod geometry identifier correction from the supplied generic identifier to `geometry.atlantic_cod`. The supplied animation JSON uses single keyframe MoLang expressions while the repository resources use deterministic numeric samples generated by `tools/bake_molang_animations.py`; the clip names, lengths, authored intent, and required motion are retained. The supplied Oceanic fin names are mapped to the existing geometry bones `Fin` and `Fin2`.

## Ordered verification

| Check | Command | Result |
| --- | --- | --- |
| Formatter and static analysis inventory | Repository task and build-script inspection | No maintained formatter or static analysis task is present. This absence is recorded and does not replace compilation or tests. |
| Forge unit and contract tests | `./gradlew :forge:test --no-daemon` | Passed. The JUnit suite completed successfully. |
| JUnit failure calibration | Temporary `Phase000CalibrationTest` run with `./gradlew :forge:test --tests tfar.bensfintasticsharks.audit.Phase000CalibrationTest` | Expected failure was reported with a nonzero exit and assertion location. The temporary fixture was removed and `./gradlew :forge:clean :forge:test --no-daemon` passed afterward. |
| Generated data | `./gradlew :forge:Data --no-daemon` | Passed and generated 302 files. The only working-tree changes were the nine pre-existing `.cache` files. No generated JSON or resource drift was introduced. |
| Forge GameTest harness | `./gradlew :forge:GameTestServer --no-daemon --rerun-tasks` and two repeat runs without task rerun | Passed on three consecutive current runs. The `bfs_combat`, `bfs_baseline`, and `bfs_movement` batches discovered and completed seven required tests. In addition to algae placement and item curiosity, the harness proves delayed Tiger bite damage, bite cooldown protection, target-loss recovery, Oceanic Whitetip passenger grab, thrash damage, timer expiry, passenger release, and Dolphin parity vertical movement for both ascent and descent. The runs completed at `2026-09-03 11:29:24`, `2026-09-03 11:29:56`, and `2026-09-03 11:33:59`. |
| Forge build | `./gradlew :forge:build --no-daemon` | Passed after the seven-test source commit. The current candidate Forge artifact passed `unzip -tqq`, contains 874 entries, and is 1,753,884 bytes with SHA 256 `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a` and SHA 512 `defa9955e0822c0f987d25195e6cbcb0fedf653e271034710bbd83566c3522ed45c5ff96624087b20670a15012b61aa0e265e321ab3620ec11a90a918ab512cf`. This is phase evidence only, not a release artifact. |
| Dedicated server smoke | `timeout --kill-after=15s 90s ./gradlew :forge:Server --no-daemon --args='--port 25576 --nogui'` under `EXT-001` | Fresh current-candidate run passed in the isolated Phase 000 worktree. The temporary target was exactly `eula=true`; the server reached `Done (3.062s)!` at `2026-09-03 14:15:24.840`, loaded Forge `47.2.0` and the mod, and terminated at the authorized timeout with exit code `124` after logging clean world saves at `14:16:33.814`. Port `25576` closed, no server process remained, and rollback restored the target to its absent pre-state. This supersedes the stale run at revision `5118a6fbdf173d962e02df49f79c3d0350bc4c8d`; the current source behavior is bound to `5dfade3cd29c7e8623608b4422e5e7ca59500536` and evidence commit `0e81b10d8d8aa26d2b1e76a95c1340ce3fc9c8a`. Full binding and rollback proof is in `phase-000-eula-session.md`. |
| Dedicated server to laptop multiplayer client | Node 1 dedicated server on port `25578` with the disposable current-candidate source, RCON-driven fixture setup, and laptop client launched with `--quickPlayMultiplayer 100.76.164.109:25578` | Passed as a current multiplayer transport and visual capture control. The server reached `Done (2.406s)!`, opened RCON on `25579`, and logged `Dev7 joined the game`. The laptop client on the NVIDIA GeForce RTX 5090 Laptop GPU logged `Connecting to 100.76.164.109, 25578`, `Connected to a modded server.`, and loaded `56` advancements. RCON set the time, weather, gamerules, tank blocks, player position, and labeled Cod, Salmon, Oceanic Whitetip, and Bottlenose Dolphin fixtures; those commands are present in both server and client logs. The hidden Xwayland capture `mp-dedicated-entity-fixture` has SHA 256 `740d1db3fc8eb05b44a3060cc1226b30c2719c4020ff1b1868229f40b5a41107` and dimensions `3800x2290`. A later RCON command `say dedicated server control probe passed` reached the laptop chat, with screenshot SHA 256 `db5a52a14b0e0a28510b82f2094c1e8881c9fba1dffba14f80e9288cd2ac20bc`, server log SHA 256 `25829b164093ee3d77b0ca417c65652034d159ea357f1fbc7828a6bfee17705b`, and laptop log SHA 256 `9a815f8dc8817a09312e7ba3eeddb8b044400e70548e117993484ef9a89d3765`. The window remained on workspace `3` while Hyprland workspace `2` stayed active before and after capture. This proves the dedicated-server-to-laptop workflow, server-side command control, and render transport without a singleplayer world, but controlled summons and exploratory probes do not close the animation, combat, cleanup, or sustained-population matrices. |
| Client loading smoke | `DISPLAY=:101 LIBGL_ALWAYS_SOFTWARE=1 ./gradlew :forge:Client --no-daemon --args='--username Dev'` on source revision `ce4918ab3784e308fb639863dbff52f23b00f801` | Historical node 1 headless client reached OpenGL 4.5 llvmpipe, Forge loading, resource reload, texture atlas completion, OpenAL initialization, the title screen, and an integrated world. It was terminated with controlled Ctrl-C. The candidate artifact SHA 256 was `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a`. No mod-specific exception or error was observed. This remains diagnostic history only and is not admissible acceptance evidence under the laptop-only window policy. Rerun on the Linux laptop before phase closure. |
| Laptop client and workspace capture | `./gradlew :forge:Client --no-daemon --args='--username Dev7'` on the Linux laptop, followed by `DISPLAY=:1 import -window <minecraft_window_id> <capture_path>` | Current artifact SHA 256 `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a` reached the title screen and integrated Forge world on the NVIDIA GeForce RTX 5090 Laptop GPU. The Minecraft window was placed on workspace 3, direct Xwayland window captures completed, and Hyprland active workspace remained 2 before and after the four-frame capture sequence. This is admissible laptop rendering and capture transport evidence, but it does not close the per-state animation, advancement, algae, population, or multiplayer matrices. |
| Interactive client baseline capture | Fresh disposable `Demo World` on the same Forge artifact, with a LAN cheats session used only to place the supplied hat in the inventory and open the advancement screen | Historical node 1 headless session loaded the mod advancement tab and rendered the root, child icon column, and `El Capitán's Legacy` tooltip. Capture `client-fresh-advancement-tree` has SHA 256 `f895c0032e530334a9ffd6d164ccf626ea5a4ce605c66c2a4ca97239452eeab2` and capture `client-fresh-root-tooltip` has SHA 256 `c79d4c16a11f34b4b50565b3f95f0574e12ba092bacae3843e50c5adad26488d`. The hat criterion completed naturally after the command-assisted inventory grant, and `client-fresh-captains-heir-tooltip` renders the complete description `Obtain Capitán Ben's Hat.` with SHA 256 `77515e770a79375ba2ab19b62d39c4541efe604e1e8ebdbe21abcb7e57ca3eb1`. After relaunch, `client-fresh-advancement-mod-tree-reloaded` rendered the mod tab title tooltip, root, and child icon column with SHA 256 `b218310e2e4fd76c41ff0a8c089a5386c80ab27f29500907abd1405167d7ffc6`. That later capture used command-granted advancement progress and is not complete seven-icon or natural completion evidence. All captures are bound to artifact SHA 256 `7fd21a6d4aa7bd76f0a00a2de056db510e8e3d288573405088dc153fe299f11e`. These captures are retained for diagnostic history only and are not admissible windowed acceptance evidence under the laptop-only policy. |
| Controlled entity capture probes | Fresh disposable `Demo World` LAN session using bounded water and dry-platform fixtures, with all command-assisted actors named in the manifest | Historical node 1 headless fish, Tiger Shark, and Oceanic Whitetip probes produced additional raw capture hashes. Cod and Salmon stayed visible for an 8 second bounded run. A named `Spin` Salmon changed silhouette and orientation across sampled frames, while the flop sample lacked strong displacement proof. A Tiger Shark changed visible position relative to a floating edible item. An Oceanic Whitetip chase started with Cod health `3.0f` and ended with no Cod entity found. These probes are exploratory diagnostic controls only and are not admissible windowed acceptance evidence under the laptop-only policy. The capture IDs and hashes are recorded in `phase-000-manifest.json`. |
| Fixed-seed world-generation sample | Disposable integrated client world `phase000-seed-240024`, seed `240024`, Overworld chunks `x=0..15`, `z=0..15`, scanned from the saved region file with Minecraft's `RegionFile` and `NbtIo` readers | Passed. All 256 target chunks generated. The scan found `35` `algae_block`, `28` `large_green_algae`, and `48` `large_red_algae` blocks, for `111` BFS algae blocks across `92` chunks, alongside `8,456` vanilla seagrass blocks. The world metadata reports seed `240024`. F3 captures identify natural algae at `(78,34,12)`, `(212,48,2)`, and `(101,55,1)` with water targets in cold-ocean biomes. Region hash, level hash, and capture hashes are recorded in `phase-000-manifest.json`. Cheats were used only for spectator travel and inspection after generation; no blocks were placed or removed. |
| Multiplayer and reconnect protocol | Two disposable Forge 1.20.1 clients using the evidence artifact. Host `Dev` opened LAN on port `46641`; guest `Guest` joined, disconnected, and rejoined through the discovered LAN entry. | Passed for the LAN join, disconnect, and reconnect protocol. The host log records `Guest joined the game` at `23:51:24`, `Guest left the game` at `23:51:44`, and a second `Guest joined the game` at `23:51:58`. Client captures show the joined world, the post-disconnect server list, and the reconnected world. The development clients reported the expected invalid-session warning and Forge still completed the modded handshake. This does not replace dedicated-server, sustained-population, or full gameplay synchronization evidence. |
| Secret scan | Tracked-text secret pattern scan | No credential or secret pattern was found. |
| Diff review | `git diff --check`, generated-resource name review, and JAR inventory | No whitespace error. The working-tree diff is limited to the protected line-ending and cache files. The JAR contains the Forge metadata, classes, generated resources, animations, geometry, textures, tags, recipes, loot, and algae world-generation resources expected by the current implementation. |

The tracked release checksum and SBOM files describe the earlier `bfs-0.24-final` artifact and are not overwritten by Phase 000. The newly built evidence JAR has a different revision and hash and must not be presented as the final release artifact. The build manifest includes a build timestamp, so evidence hashes are bound to the exact produced file and final checksums must be generated once from the merged release revision. The exact isolated EULA readiness and rollback record is in `phase-000-eula-session.md`.

## Deterministic harness coverage

`forge/src/test/java/tfar/bensfintasticsharks/audit/ReleaseContractAuditTest.java` currently proves the following without loading client classes:

* the complete Captain's Legacy description and the seven supplied advancement titles,
* one terminal punctuation mark for every generated advancement description,
* retirement of `shark_whisperer` and direct `sharks_galore` parents for `marine_biologist` and `apex_of_apex`,
* exact 16 by 16 supplied icon hashes,
* nonstatic transform values in the Cod, Salmon, and Oceanic Whitetip clips,
* Oceanic controller clip and bone references,
* retained Atlantic fish inheritance, fishing loot, cooking recipes, and exact `Spin` behavior,
* authoritative grab cleanup seams, and
* permanent algae dimensions, animation frame order, cutout models, loot, tags, and bounded placed feature settings.

`forge/src/main/java/tfar/bensfintasticsharks/gametest/BfsGameTests.java` provides seven server-safe fixtures. They prove water-preserving algae placement, no collision, water-supported survival, water restoration after removal, Tiger Shark movement toward a reachable edible Cod item, one delayed Tiger bite with cooldown protection and target-loss recovery, Oceanic Whitetip passenger grab with thrash damage and timer-based release, and Tiger Shark vertical travel matching a Bottlenose Dolphin reference during both ascent and descent without repeated direction reversal or circular horizontal orbiting. Three current `./gradlew :forge:GameTestServer --no-daemon` runs discovered all seven tests and reported seven required passes at `2026-09-03 11:29:24`, `2026-09-03 11:29:56`, and `2026-09-03 11:33:59`. The movement fixtures are regression oracles for `SharkSwimmingMoveControl`, which now follows `SmoothSwimmingMoveControl` with the Dolphin movement parameters and keeps yaw stable for vertical-only targets.

The current deterministic checks are positive controls for resource and data paths. They do not prove interactive animation, target pursuit, attack timing, tooltip presentation, multiplayer cleanup, or fixed-seed population density.

The fresh disposable client profile also provided valid advancement display and copy controls. The mod tab loaded with its root and child icon column, and the root tooltip rendered `Ben's Fintastic Sharks!` with `Achievement granted when logging in with the mod.`. After the profile received the Captain Ben hat through the normal inventory criterion, the advancement screen rendered `El Capitán's Legacy` with `Obtain Capitán Ben's Hat.`. After relaunch, the custom tab title tooltip, root, and child column rendered again in `client-fresh-advancement-mod-tree-reloaded`, SHA 256 `b218310e2e4fd76c41ff0a8c089a5386c80ab27f29500907abd1405167d7ffc6`. The earlier raw captures are identified as `client-fresh-advancement-tree`, `client-fresh-root-tooltip`, and `client-fresh-captains-heir-tooltip` with SHA 256 values `f895c0032e530334a9ffd6d164ccf626ea5a4ce605c66c2a4ca97239452eeab2`, `c79d4c16a11f34b4b50565b3f95f0574e12ba092bacae3843e50c5adad26488d`, and `77515e770a79375ba2ab19b62d39c4541efe604e1e8ebdbe21abcb7e57ca3eb1`, respectively. They are bound to the evidence artifact above. The run used command-assisted item and advancement grants in a disposable profile, so it does not substitute for natural chest discovery, full advancement completion, complete seven-icon review, or the other client state matrices.

A second fresh disposable world, `phase000-fresh-advancements`, completed all eight `sharks_galore` criteria through the normal nearby-entity trigger after eight controlled shark fixtures were summoned. The saved player advancement record reports `bensfintasticsharks:sharks_galore` with `done: true`, and the record is bound to the current candidate artifact SHA 256 `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a` by advancement-file SHA 256 `7fe926c1baa836e11f51f7e53ed3f2e9065511e1127bec6ed399c5ab5dba6178`. The same fresh profile rendered the supplied Mommy Shark icon and tooltip in `client-fresh-advancement-mommy-shark-tooltip`, SHA 256 `aff04503fda6005cdeff3d88da73c422e8afdc49da80f8caddd1c3e983a9e2d9`. This is stronger fresh-profile reachability and natural-trigger evidence, but cheats and controlled summons mean it does not prove a no-cheat natural-biome completion path or complete seven-icon visual coverage.

Additional command-assisted probes used the same artifact in bounded disposable fixtures. The Cod and Salmon run is identified by `client-fish-controlled-start` and `client-fish-controlled-video`; the named `Spin` and land `flop` controls are identified by `client-salmon-spin-start`, `client-salmon-spin-video`, `client-salmon-flop-start`, and `client-salmon-flop-video`. The Tiger Shark curiosity run is identified by `client-tiger-curiosity-start` and `client-tiger-curiosity-video`. The Oceanic Whitetip chase used a live Cod health control and is identified by `client-whitetip-cod-health-start`, `client-whitetip-cod-health-video`, and `client-whitetip-cod-health-after`. The machine-readable manifest records each SHA 256, video duration, observed result, and limitation. These captures show that the runtime fixtures and render paths can display the actors, but they do not establish every required animation state, bite timing, damage event, vertical pursuit, or cleanup postcondition.

A later stationary spectator-camera control is identified by `client-salmon-spin-spectator-start` and `client-salmon-spin-spectator-video`. The eight-second capture keeps the named `Spin` Salmon centered and shows repeated silhouette and orientation changes at one-second samples. It strengthens the positive control for the named state while leaving the complete Cod and Salmon state matrix open.

The current phase artifact also produced `client-cod-idle-labeled-current`, a five-second labeled Atlantic Cod Forge control in the disposable `phase000_animation_current` aquarium. The stationary NoAI and NoGravity fixture remained centered and stable across the capture, with video SHA 256 `aba6494077f56389832ea7d8967eb8414bdf2fad09afd23836bb2291223eddd0` and artifact SHA 256 `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a` at phase revision `5dfade3cd29c7e8623608b4422e5e7ca59500536`. This strengthens the current idle control only and does not close the required swim, fast swim, flop, death, Salmon, or Oceanic state matrices.

The same current artifact produced a close `Spin` Salmon control, `client-salmon-spin-close-current`, with video SHA 256 `60b131446d22271f4297a190833ca027213d84e09bcce0b1100764c4c4734bd0` and repeated labeled silhouette and orientation changes across the eight-second capture. A separate `client-salmon-flop-current-labeled` control placed the non-Spin Salmon on a dry stone platform, with video SHA 256 `de292b9c0b86debdd633436c44f1e9c95911f07495812a62987a158cc438bd11`. Both captures are bound to source revision `5dfade3cd29c7e8623608b4422e5e7ca59500536` and artifact SHA 256 `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a`. They strengthen the current Spin and flop controls but do not close the complete fish animation, death, or Oceanic matrices.

The current candidate revision also produced `client-fish-current-labeled-video`, a twelve-second fixed-camera control bound to artifact SHA 256 `5ea2a9e23ae0c9cd9381e19a2d0daedd5680ff4310b734fae3e0e03236195e7a` with video SHA 256 `fc1e807bbb025cae0abe034738822f87fc567db43e02291cdc661c70f2dfa3e5`. Cod, Salmon, and the named `Spin` Salmon remained labeled and visible while changing position across three sampled frames, whose hashes are recorded in `phase-000-manifest.json`. This fresh control strengthens current-artifact attribution but does not close the required per-state animation, death, or authored-transform evidence.

The fixed-seed world-generation protocol now has a saved natural sample. The disposable world `phase000-seed-240024` uses seed `240024` and contains all 256 requested Overworld chunks in `r.0.0.mca`. Minecraft's own region and NBT readers counted 35 `algae_block`, 28 `large_green_algae`, and 48 `large_red_algae` blocks, plus 8,456 vanilla seagrass blocks. Natural F3 views identify each BFS algae form in water at `(78,34,12)`, `(212,48,2)`, and `(101,55,1)`. The scan and captures are bound to the evidence artifact and are listed in `phase-000-manifest.json`. Command use was limited to spectator travel and inspection after terrain generation.

The two-client LAN protocol also passed on the same disposable world. `Dev` hosted port `46641`; `Guest` joined, disconnected, and rejoined through the LAN server list. The host log records both join events and the intervening leave event. The protocol proves login, modded handshake, cleanup, and reconnect for this integrated LAN session, while dedicated-server and sustained-population gates remain separate.

The current dedicated-server multiplayer protocol uses node 1 only for the headless Forge server and RCON control. The laptop client connects over Tailscale to `100.76.164.109:25578` with the current candidate source and NVIDIA GPU. RCON drives the disposable tank, gamerules, player teleport, and named entity fixtures without requiring a singleplayer world or a window on node 1. The client log and server log prove the modded handshake and command results, while the hidden-window capture proves that the laptop render remains available without changing EnVy’s active workspace. This protocol is now the required path for future interactive server-backed evidence; it does not by itself close the state-specific animation, combat, cleanup, or population gates.

## Defect baseline status

| Defect | Current evidence | Status | Closing evidence still required |
| --- | --- | --- | --- |
| `DEF-024-001` truncated El Capitán's Legacy description | Generated language contains `Obtain Capitán Ben's Hat.`, the contract test passes, and the fresh disposable client tooltip renders the complete description with the title `El Capitán's Legacy`. | `POSITIVELY_DISPROVED` for the deterministic and fresh tooltip paths | Final merged-artifact recheck and natural chest-discovery path remain required for release closure. |
| `DEF-024-002` Cod and Salmon animation gaps | Baked runtime resources contain moving numeric transforms for idle, swim, fast swim, flop, and Salmon `Spin`. The bounded client probes kept Cod and Salmon visible, showed orientation changes in the named `Spin` control, and showed the land `flop` control, but did not prove every state transition or unambiguous visible displacement. | `BLOCKED` | Interactive client capture proving each required state activation and visible motion. |
| `DEF-024-003` Oceanic Whitetip animation gaps | Required clips, controller references, geometry bones, and numeric motion pass deterministic checks. The current GameTest proves server-side passenger grab, active timer, thrash damage, timer expiry, and passenger release. Bounded client probes displayed an Oceanic Whitetip in water and in a Cod chase, but did not provide a complete visual transition matrix or a reliable interactive bite and cleanup oracle. | `BLOCKED` | Interactive state matrix, bite damage timeline, and cleanup evidence. |
| `DEF-024-004` Tiger Shark stationary or nonattacking behavior | The current GameTests prove movement toward a reachable edible Cod item, one delayed server-authoritative bite, cooldown protection, target-loss recovery, and vertical travel matching the Bottlenose Dolphin reference without repeated direction reversal or circular horizontal orbiting. The exploratory client curiosity run shows visible position change relative to a floating edible item, but a clean interactive pursuit and combat capture remain unverified. | `BLOCKED` | Controlled interactive pursuit, contact, damage, and recovery capture. |
| `DEF-024-005` Duplicate Shark Whisperer advancement | The generated graph omits `shark_whisperer`, preserves both child edges under `sharks_galore`, and passes cycle and parent checks. A second fresh disposable profile completed all eight `sharks_galore` criteria through the normal nearby-entity trigger, with `done: true` recorded in the saved advancement file. The controlled summon setup remains disclosed. | `BLOCKED` | No-cheat natural reachability and completion-path evidence, plus final merged-artifact recheck. |
| `DEF-024-006` Advancement punctuation and copy inconsistency | The contract test passes the complete Captain's Legacy copy, supplied titles, terminal punctuation, whitespace, and doubled-mark checks. Fresh root and Captain's Legacy tooltip captures show the expected displayed punctuation for those nodes. | `POSITIVELY_DISPROVED` for deterministic copy and captured tooltip paths | Complete fresh client tree review remains required for final closure. |
| `DEF-024-007` Remastered advancement icons absent or stale | All seven destination images are 16 by 16 and match the supplied hashes. The packaged paths are present. Fresh profiles rendered the BFS tree and a supplied Mommy Shark icon tooltip, but the viewport did not show all seven remastered icons at once. | `BLOCKED` | Complete seven-icon client display capture and final merged-artifact inspection. |
| `DEF-024-008` Algae content and generation gap | The current artifact contains all three algae resources, registrations, tags, loot, and placed features. The current GameTests pass water placement, noncollision, survival while supported, and water restoration after removal. The fixed-seed sample generated all 256 target chunks and found all three natural algae forms with a recorded vanilla seagrass control. The historical dedicated-server run passed before the common movement revision and is stale for the current candidate. | `BLOCKED` | Natural survival and collection, loot, fresh dedicated-server, client, and final merged-artifact evidence remain required. |

`POSITIVELY_DISPROVED` is used only where the deterministic contract and a positive control executed, with the fresh tooltip captures extending that control for the two observed advancement nodes. The other records remain `BLOCKED`; passing source or structural checks alone does not close a runtime defect.

## Open gates

The Phase 000 exit gate is not met. The owner-authorized `EXT-001` dedicated-server readiness and rollback gate now passes for the current candidate and is consumed. The remaining open gates include interactive state matrices, sustained population, fresh-profile completion and complete advancement-display coverage, natural algae survival and loot, multiplayer cleanup, and the final phase artifact. No persistent or production EULA setting was changed.

Interactive animation and state matrices, sustained population, a no-cheat fresh-profile completion path and complete advancement-display coverage, natural algae survival and loot, multiplayer cleanup, and the remaining visual evidence remain open. The controlled fresh profile now proves that all eight `sharks_galore` criteria can complete through the normal nearby-entity trigger, but it does not replace the no-cheat path. The fixed-seed generation, current dedicated-server readiness, and two-client LAN reconnect gates are now recorded as passed, but they do not close the broader algae, animation, or release gates. Phase integration into `envy/0.24`, the Phase 000 pull request, merge verification, and the signed annotated `bfs-0.24-phase-000` tag are not authorized until every mandatory gate is satisfied. Existing immutable tags `bfs-0.24` and `bfs-0.24-final` remain untouched.

## Current dedicated server multiplayer state probes

The current candidate was verified through a disposable multiplayer server rather than a singleplayer world. Node 1 ran only the headless Forge server on port `25578` and accepted RCON on `25579`. The Linux laptop connected over Tailscale at `100.76.164.109:25578` with the NVIDIA GeForce RTX 5090 Laptop GPU. RCON created the tank, gamerules, player camera position, and named entity fixtures on the server. The client window stayed on Hyprland workspace `3` while workspace `2` remained active, and captures were requested remotely from the laptop window. No windowed game or singleplayer world ran on node 1.

This run is bound to source revision `58c3271833dbfae04655925466f1c999541ebaa5`, artifact SHA 256 `31b9166489d50e516d5c8df9eb7b8cd3534fc156eabce54e3385c66249538163`, server log SHA 256 `afc164aabea7b208985d32d60c991cb6b4a054757f26a0ea7da35b57a29aaf6a`, and laptop log SHA 256 `6586e0cddc02862276622af57bf4ac8830e2fd099bcabd25bd25294b0553999e`. The server reached `Done (2.841s)!`, logged `Dev7 joined the game`, and delivered RCON chat markers to the connected laptop client.

| Actor and state | Capture and result |
| --- | --- |
| Cod idle | `mp-cod-idle-clean-current`, SHA 256 `3f9101856d71d432a65d9ce07b5dde794bbaca8ee44def7b49d895aa42758e4d`. The current Cod rendered in the connected laptop client. |
| Cod swim | `mp-cod-swim-static-clean-current`, 6 seconds, SHA 256 `b6ecd16a27e3329febdb9ad370ac5f83531af687a5c7827b6924c31b1687740c`. RCON reapplied motion `[0.04, 0.004, 0.01]`. |
| Salmon idle | `mp-salmon-idle-current`, 5 seconds, SHA 256 `c843d49fab114ce1333b41cfae63a8eb9fc8948ed72723f8e30ccbcb036cb98e`. |
| Salmon swim | `mp-salmon-swim-clean-current`, 6.125 seconds, SHA 256 `b6c76ed087e73b127a75100c09a857f099221a3a03bc97680834dbfcccfd238d`. RCON reapplied motion `[0.04, 0.004, 0.01]`. |
| Salmon fast swim | `mp-salmon-fast-current`, 6.125 seconds, SHA 256 `a080de4f0f6e46fa88e79298c708d79f67d161fa34f2e6de6dd5af8b66035cac`. RCON applied motion `[0.2, 0.02, 0.04]`. |
| Salmon `Spin` | `mp-salmon-spin-current`, 8.125 seconds, SHA 256 `9ed0c43971980ee4c7312d0441c109f9255b1a14718ef7382ed84ae0fc2599af`. The exact custom name produced visible silhouette and orientation changes. |
| Salmon rename recovery | `mp-salmon-rename-recovery-current`, 4 seconds, SHA 256 `a31fb8c383b86be595b307cb8bfd23f9973853eef5b01a65b9944bd0a41cc777`. Replacing the `Spin` name returned the ordinary state control. |
| Salmon flop | `mp-salmon-flop-current`, 5 seconds, SHA 256 `96a704cbc05366d8a73bd959746d0149f8015e198f88ab862460f940e9d504db`. The land control rendered on dry stone. |
| Salmon death | `mp-salmon-death-current`, 4.125 seconds, SHA 256 `8b9a034e6c7d5ea18082cb96b2fb1f45c8add04ad3470baa20850f64ede34526`. The server damage event was followed by entity removal. |
| Oceanic Whitetip idle | `mp-oceanic-idle-current`, 6 seconds, SHA 256 `ed52160bf85e963f179b1e54b229b20794a9027d8e0ef1015a22acacad0f8bcd`. |
| Oceanic Whitetip `swim_new` | `mp-oceanic-swim-new-current`, 6 seconds, SHA 256 `92ca2d33b9fd3da48441673687ffd3806c5ef6336e79568f10a3f1a6ad301d37`. RCON reapplied motion `[0.04, 0.004, 0.01]`. |
| Oceanic Whitetip `swim_fast_new` target probe | `mp-oceanic-fast-target-current`, 7.125 seconds, SHA 256 `8b1c5de767e0150184b0db2b0586590aec413307cd96259fd056322354f4ea17`. The shark reached the Cod fixture and stopped at contact. This remains a target-control probe, not a standalone fast-state oracle. |
| Oceanic Whitetip `bite_new` | `mp-oceanic-bite-new-current`, 10 seconds, SHA 256 `166e3b2136a43b87f03f4e6dd43bbdf7a59ad526246e66a78335cfdbccd4e0ea`. Cod health decreased from `33.0` to `15.5` during the synchronized bounded bite capture. |
| Oceanic Whitetip death | `mp-oceanic-death-current`, 5.125 seconds, SHA 256 `73f2914e9934d730b6e2673bb65bc24aead8a105f49671974fc54c6330ff666d`. The death pose rendered before server removal. |
| Oceanic Whitetip beached | `mp-oceanic-beached-current`, 5 seconds, SHA 256 `433b6ec65e97bb47f30a89231bee10a35f0319ba78e0244ee17e22db0b55f1bf`. The dry-platform fixture reported `OnGround` true. |
| Oceanic Whitetip natural grab and thrash | `mp-oceanic-real-grab-current`, 22.125 seconds, SHA 256 `9b031ade642c2e1b76d08c40aeb98b85f2ded22e89c13544548b94a767848d44`. Natural bite contact attached the Cod at 8.10 seconds, passenger damage continued, and release occurred at 13.05 seconds. Cod health decreased from `179.0` at attachment to `160.0` at release. |

The normal and fast fixture vectors preserve the requested approximately `0.10` vertical component ratio. These probes prove current-artifact attribution, server control, connected-client rendering, bite damage, and natural grab release. They do not by themselves close the full authored per-clip sample table, continuous body and nose orientation trace, Tiger and Bottlenose Dolphin comparison, sustained population sample, or the remaining disconnect, dimension, passenger, camera, and equipment cleanup matrix. Those gates remain `BLOCKED` in the manifest.

The repeatable commands and metadata schema are documented in `docs/test/phase-000-runbook.md`. The complete machine-readable hashes and archive member inventory are in `docs/verification/phase-000-manifest.json`.

The `725bab666d38cd3f05e8e618f02fc96a6b84d7c7` resource revision also has a deterministic five-point sample comparison against every supplied 0.23 Cod, Salmon, and Oceanic Whitetip MoLang channel. Samples were evaluated at start, quarter, half, three-quarter, and end using the authored clip length and compared with the baked destination keyframe at the same time. The largest absolute delta is `0.00000490538903` for Cod swim, below the `0.0001` tolerance. Oceanic Whitetip `swim_new` retains its authored `1.7083` second endpoint and has a maximum delta of `0.00000486971982`. Static and already-keyed clips remain listed explicitly rather than being misclassified as sampled MoLang channels. The complete per-clip counts, hashes, and bone mapping are in the `animation_sample_equivalence` manifest entry.

## Latest dedicated multiplayer capture

The latest candidate was verified through a disposable dedicated multiplayer server. Node 1 ran only the headless Forge server from the phase worktree on port `25578`, with RCON on `25579`, world `phase000-mp-final-2`, and the authorized disposable `eula=true`. The Linux laptop connected over Tailscale at `100.76.164.109:25578` with the NVIDIA GeForce RTX 5090 Laptop GPU. The candidate source revision is `725bab666d38cd3f05e8e618f02fc96a6b84d7c7`, and the phase artifact is 1,754,085 bytes with SHA 256 `d478700bd3ea6faab6393e7d658fbdb9c2ecb4a50404d1349c617089dc5579f3` and SHA 512 `2077a5f8f4d42f9e2f5de87dbee3288fdb3cf8e47f58da90682a9037ff6850b08821648493e0afb648f6dc447724e199237fbd42b56aa4d82824c379e74dd3d6`. The JAR passed `unzip -tqq`.

RCON disabled natural spawning with `doMobSpawning=false`, filled a bounded water tank, summoned labeled Oceanic Whitetip and Bottlenose Dolphin fixtures, and teleported the client player into the tank. The server reached `Done (13.002s)!` at `23:09:13.050`, logged the client join at `23:20:49.392`, and sent `say phase 000 multiplayer animation capture complete` at `23:24:40.790`. The laptop log contains `Connected to a modded server.`, `Loaded 56 advancements`, and the matching RCON chat marker at `23:24:40.807`. The captured server log SHA 256 is `98be9a44c11aba062f8ef7e51f4a7629dff535387e7479278f6844cbc738ded5`; the captured laptop log SHA 256 is `b581ec0f0e381742637fc36e367b2e426e6aa3c319303fb72d5a0349b0e434ea`.

The client window remained on Hyprland workspace `3` while workspace `2` stayed active before and after capture. All captures were requested remotely from the laptop Xwayland window without switching the active workspace. `mp-final-fixtures` has SHA 256 `c8744c966a2217f9d2d2c7c353a15443b7c8bdba452660adccd3b2ff5aa6ff1e` and dimensions `3800x2290`; it shows both labeled fixtures rendered in the connected server world. Two subsequent server-controlled position samples have SHA 256 `73009fa8b52a2b4bf1a9294f7e062c73a9a4bf2a4c04614e7b730365b52862f5` and `5c8c78679211f6ef44d22b869d90184ad62901faf3049e074f68ed50d9fdefcd`, both `3800x2290`. The first sample placed the shark at `-6,65,0` and dolphin at `6,65,0`; the second placed the shark at `-1,68,0` and dolphin at `1,62,0`. These are controlled server-state comparisons, not a continuous natural-motion oracle. The 12.05 second laptop X11 transport capture has SHA 256 `77ca225ec607145f7b4024b4e4b8ce92bc619fb1d4408fc502b2279c39fcf77e`; its repeated hidden-window backing frame means it is retained as transport evidence only and is not used to claim continuous motion.

This run proves the required node 1 headless-server to Linux-laptop client route, Tailscale connectivity, RCON fixture control, connected-world rendering, and workspace-safe capture. It does not close the interactive per-state animation, continuous body and nose orientation, combat, cleanup, or sustained-population gates. The phase remains open.
