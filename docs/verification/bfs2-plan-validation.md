# Debug and Content Plan Validation

The [implementation plan](../general/plan.md) and all eight registered phase blueprints passed structural, diagnostic, handoff, research fingerprint, and semantic planning checks on September 12, 2026. This verifies the planning instructions. Gameplay implementation, performance acceptance, and client verification have not started for this pass.

The plan set SHA 256 is `62f0518259f6423afc4ddb0c84c69115f25e555e8dba4ae94206769e26cdaa03`. The master SHA 256 is `c763a41ce499288783df1600b9634477e43aca5c1f73740ceb33f747af339bc6`. The [manifest](../general/plan.index.json) registers nine plan files. The [handoff](../general/plan.handoff.json) preserves the resolved intake and completion endpoint.

## Corrected Instructions

| Phase and stable IDs | Correction and verification |
| --- | --- |
| [Phase 001](../general/phases/plan-phase-001.md), BFS2-REQ-003, BFS2-REQ-004, BFS2-REQ-022, P001-TASK-002, P001-TASK-004 | Base pairs now change only one coefficient at a time. Expected powered displacement is calculated independently from normalized intent, state, bases, and multipliers. Each complete 200 tick measurement starts after its 100 tick settling period. |
| [Phase 005](../general/phases/plan-phase-005.md), BFS2-REQ-014, BFS2-REQ-015, BFS2-REQ-016, BFS2-REQ-022, P005-TASK-002 through P005-TASK-004 | Density bounds consistently cover 16 chunks and 64 by 64 blocks. Baseline occupancy remains an explicit unproven preflight gate. Client capture begins after connection and joined world confirmation. Ordinary growth may fill the last source water cell; the additional water above the top applies only to natural generation. Age 25 stops random growth only. |
| [Phase 006](../general/phases/plan-phase-006.md), BFS2-REQ-018, BFS2-REQ-019, BFS2-REQ-022, DEC-004, P006-TASK-001, P006-TASK-004 through P006-TASK-006 | A 7200 tick capture contains the full oxygen interval, first empty tick, natural exit, and 300 refill ticks. Independent bounded counters prove every decrement and refill without a long per tick travel log. Milestone readiness precedes implementation, and oxygen dependencies point to DEC-004. Short input fixtures receive their own complete recording windows. |
| [Phase 007](../general/phases/plan-phase-007.md), BFS2-REQ-021, P007-TASK-006 | Final testing artifacts remain in a guarded stable directory under the project anchor, outside disposable worktrees. Hash readback precedes eligible worktree teardown. Client connection, audio isolation, and capture ordering follow the same entry contract. |

## Validation Results

The complete registered set has eight contiguous phases, 22 requirements, 22 canonical owners, 22 acceptance criteria, ten decisions, two prerequisites, and 56 evidence items. Shared interface projections match their frozen canonical declarations. Installed file fingerprints match the reviewed candidate, including the deterministic handoff. Both the maintenance reconstruction and full diagnostics validator pass against the installed paths. The complete research package reports `EVIDENCE_READY` with no pending source recheck.

Independent arithmetic confirms the movement example for normalized intent `(3/5,4/5,0)`: base pairs `(2,6)`, `(6,6)`, and `(6,2)` produce expected cruise steps `(0.06,0.24,0)`, `(0.18,0.24,0)`, and `(0.18,0.08,0)` blocks per tick. Sprint pairs likewise isolate one multiplier. Actual adapter displacement remains a future gameplay assertion.

The density dimensions satisfy `4 * 4 = 16` chunks and `4 * 16 = 64` blocks per side. Oxygen interval arithmetic was checked for every one of the 200 permitted entry offsets and 600 permitted exit offsets. The latest required refill observation is capture offset 7098, inside the 7200 tick window. A real run must also satisfy survival, continuity, ordinary movement, and complete evidence requirements.

The master, phases 000, 002, 003, and 004, phase ownership, interface signatures, research, resolved decisions, and supplied image bytes remain unchanged by these corrections. Only the four affected drafts changed. The initial handoff, documentation index, this validation record, and a narrowly scoped Markdown hard break attribute complete delivery. Every trailing whitespace occurrence in the new planning Markdown was checked to be exactly two spaces on a nonempty line.

The validator retains one nonblocking warning about the 17,834 word master. The bounded correction preserves its existing architecture. No goal or active phase cursor was created. The next declared action is P000-TASK-001, which revalidates current source, protected changes, remote state, issue 28, and historical evidence before implementation or runtime allocation. The historical performance rejection remains failed and open.
