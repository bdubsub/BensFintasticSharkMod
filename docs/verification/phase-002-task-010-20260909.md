# Phase 002 Task 010 species profile manifest

Date: September 9, 2026

Task 010 now has one revision bound manifest at `docs/verification/phase-002-task-010-profile-manifest.json`. It covers the 22 living registry entries, their scientific identities, evidence confidence, locomotion modes, body-length conventions, retained horizontal baselines, candidate vertical and angular profiles, motion writers, animation resources, renderer bindings, action ownership, and focused positive and negative fixtures.

The manifest distinguishes direct observations, institutional summaries, related-species inference, retained implementation values, and engineering candidates. Every candidate has an explicit unit and status. Unknown biological maxima remain unknown and are not used as a numeric acceptance claim. The owner-requested vertical slowdown is recorded once as the DEC-006 historical constraint. It is not multiplied onto non-fish animals.

The action catalog gives every mapped action its sensory preconditions, deterministic ranking, memory lifetime, cardinality, movement owner, completion condition, timeout, preemption, cleanup, retry policy, presentation binding, and positive and negative fixture. The species rows name only catalog actions that apply to that animal. Motion writers are listed by class, including the shared shark path where it applies, so later behavior work cannot silently add a second writer.

The new `SpeciesProfileManifestTest` checks the schema, exact 22-entry coverage, duplicate and unknown IDs, numeric and unit-bearing profile fields, action catalog completeness, motion-writer identity, animation and renderer bindings, and fixture presence.

Command:

```text
./gradlew :forge:test --no-daemon --console=plain
```

Result: `31` tests completed, `0` failures, `0` errors. The test ran on node 1 with Java 17. The manifest closes the Task 010 inventory and calibration-ledger gate. It does not claim that the candidate profiles, species behaviors, client visuals, multiplayer lifecycle, or owner DEC-015 review have passed. Those remain downstream Phase 002 work.

Manifest SHA-256: `7a6f19e4b0792dd10d8834424591762cc08e1071340809aca85456336eaee357`.
