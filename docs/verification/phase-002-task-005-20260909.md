# Phase 002 Task 005 Oceanic Presentation Ownership

Date: September 9, 2026

The Oceanic Whitetip presentation controller now yields while a server-authoritative grab has both a positive grab timer and a living passenger. This keeps the locomotion controller from competing with the dedicated thrash controller over overlapping bones. A timer without a passenger, or a passenger without a positive timer, does not enter the grab presentation.

The death trigger is now edge-triggered at the first death tick instead of being requested on every subsequent tick. The existing server death cleanup remains authoritative and still removes the entity at the established terminal tick.

The Oceanic bite delay is five server ticks. This is the authored jaw impact at 0.25 seconds in the 0.75 second `bite_new` clip, so the server damage frame is tied to the visible jaw peak rather than an arbitrary later tick.

The focused presentation state matrix passed in the Forge test suite:

```text
./gradlew :forge:test --no-daemon --console=plain
BUILD SUCCESSFUL in 11s

29 tests completed, 0 failures
```

This evidence covers the pure grab presentation predicate and compile-time integration. The required tick-level bite trace, server GameTests for every cleanup path, packaged-server run, laptop visual approval, and multiplayer observations remain open for the later Task 005 and phase verification gates. The earlier Phase 002 entry run also recorded four known failures and was not treated as a pass.
