# Phase 002 Task 003 Resource Bake and Structural Validation

Date: September 9, 2026

The supplied 0.23 Cod, Salmon, and Oceanic Whitetip animation files were extracted into a disposable directory and transformed only on controlled copies. The protected archives were not changed. The source shape inventory was 48 wrapped MoLang channels for Cod, 39 for Salmon, and 35 for Oceanic Whitetip. The required clip inventory was four Cod clips, five Salmon clips, and seven Oceanic Whitetip clips.

The Oceanic `beached` source clip omits an animation length. The existing destination resource already records the authored four second loop, so the controlled copy used that existing value as the compatibility input. No new duration or movement value was invented. The fail closed baker still rejects the unqualified source copy and therefore cannot silently choose a duration.

## Controlled bake

The targeted bake transformed 122 channels:

| Resource | Channels | Controlled baked SHA-256 |
| --- | ---: | --- |
| Cod | 48 | `ffa889ee5418b1439e4b1f6865404b02c4adfd5a6a03cc7e9df6f4b363c125ab` |
| Salmon | 39 | `f7670f1eb24182309c536e032a412da69f6ddea57748c62f93dae8d1e4d6faed` |
| Oceanic Whitetip | 35 | `beeafb6924f618270d869a9a61306061c53f2ae868d3b1f97589890a3d1e2025` |

A second invocation over the same controlled copies returned zero channels for all three files and produced no byte changes. Cod and Salmon controlled outputs match their current destination resources exactly. Oceanic uses the documented `leftFin` to `Fin` and `rightFin` to `Fin2` geometry mapping, so its controlled source filename and bone names are expected to differ from the destination path.

## Destination correction and audit

The existing Oceanic `beached` clip had the two documented fin bindings reversed and placed the right fin position on `Fin`. The destination now maps the source `leftFin` rotation to `Fin`, the source `rightFin` rotation and position to `Fin2`, and keeps the existing four second loop. No other Oceanic clip or geometry binding was changed.

The five point sample audit at start, quarter, half, three quarter, and end reports no missing mapped channels and a maximum absolute sample error of `0.0000000000` across all three resources. Every required clip has a moving destination witness above the `0.0001` variance threshold. Cod and Salmon retain every required clip and exact `Spin` identifier. Oceanic retains idle, normal swim, fast swim, bite, death, beached, and thrash.

Final resource hashes after the binding correction are:

| Resource | SHA-256 |
| --- | --- |
| Cod animation | `ffa889ee5418b1439e4b1f6865404b02c4adfd5a6a03cc7e9df6f4b363c125ab` |
| Salmon animation | `f7670f1eb24182309c536e032a412da69f6ddea57748c62f93dae8d1e4d6faed` |
| Oceanic Whitetip animation | `2f7d7399b4c165c5def46df0d3fb1027c54b49a13e1e9398dc4e4581860e12ee` |
| Oceanic Whitetip geometry | `c8cc1fd88ad8df8ce528c4cbcf63a8faff2ebd88e166cb124051029048d4017e` |

The controlled extraction, generated files, audit output, and temporary directories were removed after the final comparison and cleanup was verified. Interactive visual state approval and controller reachability remain later Phase 002 gates.

After the resource correction, the focused Forge check passed with Java 17:

```text
./gradlew :forge:compileJava :forge:test --no-daemon --console=plain
BUILD SUCCESSFUL in 11s
```
