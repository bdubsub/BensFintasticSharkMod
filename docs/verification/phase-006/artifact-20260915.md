# Phase 006 artifact evidence

The final Phase 006 candidate was built from source commit `7219dfa` with Java 17, Forge 47.2.0, and the checked in Gradle wrapper.

```text
./gradlew :forge:build --no-daemon
```

The Forge artifact is `forge/build/libs/BensFintasticSharks-forge-1.20.1-1.0-rc.1.jar`.

```text
sha256  f2a126f40f8dd6821b2c4213edea5ca049e15420b0f2db2c3c2dc01a26306cfe
sha512  79eb6c9e3fdc5763ebec2205af90db93e537cd9e325cf0e0159bc2e8d4d0baed5e650e726fa7ad9e5e381c1132b7993063b3a0b2367f048b670b4f861f5e3c4f
```

`unzip -tqq` passed. The archive contains the dive armor geometry, supplied atlas and slot textures, generated item models, the follow stick model, the dive and follow classes, and the Forge metadata. No dive recipe is present. The data generation task passed without a generated resource diff. The complete unit test task and debug parser self tests passed, with 31 parser tests passing.

The non follow server namespace completed all 95 required GameTests after the tiger curiosity fixture allowed its own water entry disturbance to settle before the item assertion. The follow namespace remains at 36 of 36 required tests, and the five test dive namespace remains passing. These server checks do not close the packaged client or laptop gates.

This artifact is a source bound candidate for the remaining packaged server and laptop gates. It is not a release publication.
