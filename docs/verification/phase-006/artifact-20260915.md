# Phase 006 artifact evidence

The final Phase 006 candidate was built from source commit `a2213a7` with Java 17, Forge 47.2.0, and the checked in Gradle wrapper.

```text
./gradlew :forge:build --no-daemon
```

The Forge artifact is `forge/build/libs/BensFintasticSharks-forge-1.20.1-1.0-rc.1.jar`.

```text
sha256  e0f2a3ccd6c9a6e8da12e6d243dee82c7eac00383cf49eade5251e7b01be3214
sha512  2e75d19fbe9299ebc9effe1c38537f52ed49b73bf77b9a82271234d0fefdd3deb5140c72b59e82a0f860c9617542ca52f9cd55e45bc519cee64105ec04147114
```

`unzip -tqq` passed. The archive contains the dive armor geometry, supplied atlas and slot textures, generated item models, the follow stick model, the dive and follow classes, and the Forge metadata. No dive recipe is present. The data generation task passed without a generated resource diff. The complete unit test task and debug parser self tests passed, with 31 parser tests passing.

The non follow server namespace completed all 95 required GameTests after the tiger curiosity fixture allowed its own water entry disturbance to settle before the item assertion. The follow namespace completed all 36 required tests, including the retained arrived target and fresh click release regression. The seven test dive namespace passes its lifecycle matrix. The complete server sweep then completed all 131 required tests in the disposable `bfs2-p006-full-20260915-final` runtime. The runtime was removed and its absence was verified after the run. The sweep emitted existing fixture chunk cleanup warnings during shutdown and one expected fishing delivery rejection warning, but no GameTest failed.

The rebuilt candidate also passed the installed Forge server gate on node 1. The disposable runtime used Java 17, Forge 47.2.0, the matching GeckoLib 4.4.7 and SmartBrainLib 1.14.2 jars, `eula=true`, online mode, and port 25906. The server reached `Done (14.626s)` and followed the normal save and shutdown path under the bounded timeout. The candidate, GeckoLib, and SmartBrainLib SHA 256 values were `e0f2a3ccd6c9a6e8da12e6d243dee82c7eac00383cf49eade5251e7b01be3214`, `6601d1911b80580dd2eb4b0ff754fc0ae2f0a62cfd4dfb6ecd8d145c87a81ec0`, and `3f0609e603181acf9006a5f7636dc2d75ab1b19f4a38ac6c6aa4ce548916146b`. The sanitized server transcript SHA 256 was `1bc1e891996dcb4d549b6038cb6ca776b9398e185cbec3d56000d2ad764a41cb`. The runtime and copied artifacts were removed and their absence was verified.

The packaged server gate is closed. The laptop visual and input gate remains open until the exact candidate is exercised on `envision` with the required silent client procedure.

This artifact is a source bound candidate for the remaining laptop gate. It is not a release publication.
