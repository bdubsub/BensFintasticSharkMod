# Phase 002 follow lease verification

This record covers the current Phase 002 follow lease implementation. It is a partial evidence packet and does not close Phase 002. The broad controller inventory, pinned third party artifact exercise, laptop right click gate, and full phase integration remain open.

## Candidate and environment

The current candidate is the `envy/bfs2-phase-002` worktree under `/mnt/hermes/projects/BFSMOD/_qa/windows-startup-fix`. The packaged candidate is bound to source commit `5435ba8f494a6604002cffb741fd689b1e50b485`. The supported toolchain is Minecraft 1.20.1, Forge 47.2.0, Java 17, GeckoLib 4.4.7, and SmartBrainLib 1.14.2.

The isolated server checks ran on `node-1` as headless server work. Each run used a disposable runtime under `/tmp`, wrote `eula=true` before launch, and removed the exact runtime after the final log consumer. No client was launched.

## Implemented behavior

The Forge side now registers a server owned follow lease manager and a reusable marked stick item. The marker stores a namespaced version, issued session identifier, and owner reference. The server validates the operator, marker, held hand, live `Mob` target, dimension, range, interaction deduplication, one owner and one target arbitration, and the thirty two lease cap before claiming.

The lease uses a controller adapter every ten ticks. Ordinary mobs use normal navigation. SmartBrainLib entities use a temporary `WALK_TARGET` memory and restore the prior target only when the lease still owns that memory. It does not teleport the target or set direct velocity. Arrival, timeout, blocked progress, range, marker loss, permission loss, death, removal, logout, dimension change, unload, shutdown, and explicit stop release the lease and stop only its owned intent. Repeated interaction callbacks for the already claimed target are idempotent and keep the lease active. The issued marker remains reusable during the server session. Server shutdown clears issuance and lease state, so an old marker cannot claim after restart.

Follow diagnostics use the existing `bfs-debug-v2` writer. The category records claim, adapter, intent, progress, block, rejection, release, and restoration events with pseudonymous owner and target values. The parser validates the event fields and rejects raw UUID leakage.

## Deterministic checks

The following checks passed after the latest source changes.

* `./gradlew :forge:compileJava :forge:compileTestJava --no-daemon`
* `./gradlew :forge:test --no-daemon`
* `./gradlew :forge:Data --no-daemon`
* `python3 -B -m unittest tools/test_bfs_debug_analyze.py`, 20 tests passed
* `git diff --check`

The focused follow GameTest run passed all four follow fixtures, including the SmartBrainLib Harbor Seal memory ownership and restoration assertions.

```text
./gradlew :forge:GameTestServer --no-daemon --rerun-tasks \
  -PbfsGameTestRunDir=/tmp/bfs2-phase-002-follow-gametest-20260913 \
  -PbfsGameTestNamespaces=bfsfollow
```

The server reported `follow_arbitration`, `follow_session_reset`, `follow_claim`, and `follow_controller_families`, then shut down successfully. The runtime was removed and verified absent. The authorization reset fixture invalidates the persistent session binding without clearing shared server state; a real process restart remains an open lifecycle gate. The earlier broad GameTest run was stopped while an unrelated long existing batch was active. It is not used as Phase 002 completion evidence.

## Third party preflight

The pinned external artifact is Alex's Mobs `1.22.9` for Minecraft `1.20.1` Forge, downloaded from its official CurseForge file record at `https://www.curseforge.com/minecraft/mc-mods/alexs-mobs/files/5698791`. The direct distribution URL, exact hashes, LGPLv3 license, entity id `alexsmobs:grizzly_bear`, Citadel `2.6.0` dependency, and controller path are recorded in `phase-002-follow-manifest.json`. Both archives passed `unzip -tqq`. Static inspection found the declared Forge metadata, the `EntityGrizzlyBear` goal and movement classes, and no download or process launch hook in the reviewed string set.

A packaged Forge `47.2.0` server using Java `17.0.19`, the current BFS jar, GeckoLib `4.4.7`, SmartBrainLib `1.14.2`, Alex's Mobs, and Citadel reached `Done` and stopped cleanly. The development GameTest path was not accepted for this artifact because Citadel's mixin refmap targets official server names that are absent from the Parchment mapped userdev classpath. The packaged startup proves loading compatibility only. The actual external mob follow interaction remains open until the dedicated server and laptop click gate are exercised together.

The Forge build passed with `./gradlew :forge:build --no-daemon`. The locally inspected Forge jar was `forge/build/libs/BensFintasticSharks-forge-1.20.1-1.0-rc.1.jar`. Its current hashes are:

```text
sha256 62d1da6e7b6948d15d97970a445310f3d7334ff8b5fa07160968a4d2abc87e6f
sha512 95cc39ddd79e3f236e4150b0e9162b2b881a12128dafb8ef0ea392f4c28c229ea27df449d193934ba38633cf65d57d39d26ca37cfcb0dec7dea55125fd9176b8
```

The jar passed `unzip -tqq` and contains the follow manager, both follow GameTest classes, and generated follow stick model. The hash is bound to the current local build and must be rebound to the merged phase commit before integration.

## Security review

The scoped review found no confirmed exploitable finding. The command remains permission level two, interaction handling is server side, markers are bound to the current server issuance and owner, operator status is checked again during interaction and ticking, duplicate callbacks are deduplicated, and leases are bounded to 32 entries, 64 blocks, 2,400 ticks, and 200 blocked ticks. Follow diagnostics pseudonymize owner and target values. The parser tests reject raw UUID leakage. Residual risk is the unverified third party follow interaction, independent fixture, restoration oracle, and client gate.

## Open gates

The compatibility manifest records all active registry ids, named boss fixtures, the SmartBrainLib adapter, and a pinned Alex's Mobs artifact with production startup evidence. The independent non BFS fixture, third party follow interaction, and ordinary controller restoration within forty ticks remain open. The focused fixtures prove the server lease boundary and arbitration, but they do not prove every controller family.

The required laptop gate remains unverified. The laptop client loaded the candidate with the pinned Alex's Mobs and Citadel jars, joined the disposable dedicated endpoint, and had its exact Java audio stream muted. The client then timed out before the server observed an entity interaction, so no right click or moving owner claim is accepted as evidence. The temporary server was stopped and removed. The modified laptop instance still requires restoration from its recorded backups when the private connection is available. Phase integration, merge, default branch verification, tag, wiki synchronization, and final artifact delivery remain blocked until those gates are complete.
