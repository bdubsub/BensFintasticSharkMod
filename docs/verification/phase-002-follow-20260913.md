# Phase 002 follow lease verification

This record covers the current Phase 002 follow lease implementation. It is a partial evidence packet and does not close Phase 002. The pinned third party follow exercise, real process restart invalidation, and full phase integration remain open.

## Candidate and environment

The current candidate is the `envy/bfs2-phase-002` worktree under `/mnt/hermes/projects/BFSMOD/_qa/windows-startup-fix`. The packaged candidate is bound to source commit `b146dd10312366e3db1118328fcb5aff4ec49ab9` and jar SHA256 `69d76f0409dcd361f7618674348352c9b96e3a02ef117c79e9a217705eb1ab7a`. The supported toolchain is Minecraft 1.20.1, Forge 47.2.0, Java 17, GeckoLib 4.4.7, and SmartBrainLib 1.14.2.

A fresh headless verification build from the current branch tip `7ac993e` passed after the recorded live run. Its rebuilt jar SHA256 is `038aa4c1d7a32bafcb138f24b94c402ddd596cd3f54cce737ccbee172ee0a845` and SHA512 is `044c0cc5c58dde4eda0e678e77e26f72e835e6437799d5e7b3598f40941bd30e7e15074227580f1d364adda2909d3e1da146bcc923be94299b6c504f4ba2bf19`. The live captures below remain bound to the earlier packaged hash and are not silently relabeled.

The isolated server checks ran on `node-1` as headless server work. Each run used a disposable runtime under `/tmp`, wrote `eula=true` before launch, and removed the exact runtime after the final log consumer. No client was launched.

## Implemented behavior

The Forge side now registers a server owned follow lease manager and a reusable marked stick item. The marker stores a namespaced version, issued session identifier, and owner reference. The server validates the operator, marker, held hand, live `Mob` target, dimension, range, interaction deduplication, one owner and one target arbitration, and the thirty two lease cap before claiming.

The lease uses a controller adapter every ten ticks. Ordinary mobs use normal navigation. SmartBrainLib entities use a temporary `WALK_TARGET` memory and restore the prior target only when the lease still owns that memory. It does not teleport the target or set direct velocity. Arrival, timeout, blocked progress, range, marker loss, permission loss, death, removal, logout, dimension change, unload, shutdown, and explicit stop release the lease and stop only its owned intent. Repeated interaction callbacks for the already claimed target are idempotent and keep the lease active. The issued marker remains reusable during the server session. Server shutdown clears issuance and lease state, so an old marker cannot claim after restart.

Follow diagnostics use the existing `bfs-debug-v2` writer. The category records claim, adapter, intent, progress, block, rejection, release, and restoration events with pseudonymous owner and target values. The parser validates the event fields and rejects raw UUID leakage.

## Deterministic checks

The following checks passed after the latest source changes.

* `./gradlew :forge:compileJava :forge:compileTestJava --no-daemon`
* `./gradlew :forge:test --no-daemon`
* `./gradlew :forge:Data --no-daemon`
* `python3 -B -m unittest tools/test_bfs_debug_analyze.py`, 20 tests passed
* `git diff --check`

The focused follow GameTest run passed all thirteen follow fixtures, including the SmartBrainLib Harbor Seal memory ownership and forty tick restoration assertions, the independent non BFS controller fixture, the moving owner route around a two block obstacle, the target death release lifecycle fixture, and the held click arrival latch regression.

```text
./gradlew :forge:GameTestServer --no-daemon --rerun-tasks \
  -PbfsGameTestRunDir=/tmp/bfs2-phase-002-nav-7KklvQ \
  -PbfsGameTestNamespaces=bfsfollow
```

The server reported `follow_restoration`, `follow_arbitration`, `follow_session_reset`, `follow_security_lifecycle` with marker loss, permission loss, range, and blocked route exits, `follow_navigation`, `follow_independent_fixture`, `follow_lifecycle`, `follow_claim`, and `follow_controller_families`, then shut down successfully. The runtime was removed and verified absent. The navigation fixture uses a normal zombie navigation path, moves the owner around a two block wall, and verifies reduced distance while the target remains alive. The restoration fixture keeps the SmartBrainLib target stable for forty ticks, then verifies the lease remains released, the owner walk target is not retained, and target health and entity identity are unchanged. The independent fixture is a test-only Mob class with its own controller class and a non BFS registered type, and it acquires and releases through the generic navigation adapter. The authorization reset fixture invalidates the persistent session binding without clearing shared server state; a real process restart remains an open lifecycle gate. The target death fixture proves the lease is released on the next server tick. The earlier broad GameTest run was stopped while an unrelated long existing batch was active. It is not used as Phase 002 completion evidence.

The interaction listener now receives canceled Forge entity interaction callbacks so target mob handlers cannot hide the debug marker interaction. The focused follow test run passed after this change. A second laptop run reached the dedicated server with the candidate jar and the exact Java stream muted. The server granted operator status and accepted the marker command, but no click claim was recorded because the disposable fixture was repeatedly reset during manual setup. That run is not accepted as click evidence.

A later aligned laptop run used the same candidate jar with Alex's Mobs and Citadel on both sides. The player joined the disposable server and the exact Java stream was muted. The first persistent zombie died from vanilla daytime burning before interaction, and a later setup left the player on the vanilla death screen after the zombie attack. No follow claim was recorded and no entity was removed by the follow manager. This run is also not accepted as click evidence. The observation explains the reported disappearance in this fixture as ordinary entity death rather than follow lease cleanup.

The later production fixture reproduced the same distinction. Newly summoned Alex's Mobs animals disappeared before a lease existed, while a persistent villager remained available for interaction. The follow manager has no teleport, discard, kill, or broad entity removal path. The disappearing animals are therefore fixture lifecycle behavior, not the follow route moving them somewhere else. The production fixture was stopped and its temporary world and client state were removed after this observation.

## Third party preflight

The pinned external artifact is Alex's Mobs `1.22.9` for Minecraft `1.20.1` Forge, downloaded from its official CurseForge file record at `https://www.curseforge.com/minecraft/mc-mods/alexs-mobs/files/5698791`. The direct distribution URL, exact hashes, LGPLv3 license, entity id `alexsmobs:grizzly_bear`, Citadel `2.6.0` dependency, and controller path are recorded in `phase-002-follow-manifest.json`. Both archives passed `unzip -tqq`. Static inspection found the declared Forge metadata, the `EntityGrizzlyBear` goal and movement classes, and no download or process launch hook in the reviewed string set.

A packaged Forge `47.2.0` server using Java `17.0.19`, the current BFS jar, GeckoLib `4.4.7`, SmartBrainLib `1.14.2`, Alex's Mobs, and Citadel reached `Done` and stopped cleanly. The development GameTest path was not accepted for this artifact because Citadel's mixin refmap targets official server names that are absent from the Parchment mapped userdev classpath. A dedicated server and laptop run then recorded a real click claim for `alexsmobs:grizzly_bear` with the current candidate jar, normal navigation adapter, arrival release, restoration, and arrival latch. The first capture was before the server runtime binding fields were added, so the follow event is retained as behavior evidence and the later bound capture is retained as artifact evidence.

The Forge build passed with `./gradlew :forge:build --no-daemon`. The locally inspected Forge jar was `forge/build/libs/BensFintasticSharks-forge-1.20.1-1.0-rc.1.jar`. Its current hashes are:

```text
sha256 d0725a88e2ac7789d62d08279f1482550dbfacd941f032bd6f30e05970f7a366
sha512 8395c049693c3478f497834fa77f6096f51b80c4b1c44dc0a5c4c23606ef979fe5918c47839ab7fa1c1bca4038f9e0738078e15202c8c913f8c44882b805f0e1
```

## Rejection feedback patch on 2026-09-14

The follow callback now sends one chat message per server tick when a click is rejected. It identifies missing operator permission, an invalid or stale issued marker, an unsupported target, range, lease ownership, lease capacity, and the short arrival cooldown. This preserves the permission level two contract and does not change route ownership or movement behavior. The patch is source commit `faefe42cfe26c1d590f3191efecadaae5cb35289`. Its rebuilt Forge JAR passed `unzip -tqq`, with SHA256 `48e5354b41093a467dde09c5e01e6ac3e0ad7bf48de10d511bdc8ab156e6c67a` and SHA512 `414dd310921cdcdfaf1be40505e1007b7a7b49e87b5ddfa6b8889fd3f29da9da9b403254d5c0657586d723bdaa0be441a8278212d7f2e21a2b06901636f716b1`.

The compile, focused `bfsfollow` GameTest run with 19 required tests, Forge build, archive integrity, and diff checks passed after this patch. No new laptop click or moving follow evidence was accepted. The production input gate remains open until a real issued stick claim and sustained moving follow are recorded.

The fresh headless rebuild was also checked with `unzip -tqq`. The compile, unit test, focused follow GameTest, build, parser, and diff checks passed. The focused GameTest run again reported thirteen required tests passed, and its disposable run directory was removed and verified absent.

The jar passed `unzip -tqq` and contains the follow manager, both follow GameTest classes, and generated follow stick model. The hash is bound to the current phase candidate build and must be rebound to the merged phase commit before integration.

For evidence correlation, the previous laptop jar at SHA256 `f0d839239c7a9b0efc68a83fb9837b09b75dad3c1d05f696ba413158d7cc02ef` was compared read only with the current candidate. The production class and resource tree, excluding the manifest and follow GameTest classes, has the same sorted content digest `b63633b032d814f9ec90840973799c83abc5fdc4a52bbf3c7a1fde7628e382a3` across 834 files. The only differences are the build manifest and the follow GameTest classes. This supports the production behavior comparison below, but it does not close the exact current candidate laptop gate.

## Security review

The private review covered the follow command entry point and `BfsFollowManager` at source commit `91c854c`. It found no confirmed exploitable finding. The command remains permission level two, interaction handling is server side, markers are bound to the current server issuance and owner, operator status is checked again during interaction and ticking, duplicate callbacks are deduplicated, and leases are bounded to 32 entries, 64 blocks, 2,400 ticks, and 200 blocked ticks. Follow diagnostics pseudonymize owner and target values, and the parser tests reject raw UUID leakage. The review also checked stale and forged marker rejection, owner and target arbitration, lifecycle release, and token scoped controller restoration. Residual risk is the unverified third party follow interaction and real process restart invalidation. No source, dependency, credential, archive, or command injection finding was confirmed in this scope.

## Open gates

The compatibility manifest records all active registry ids, named boss fixtures, the SmartBrainLib adapter, and a pinned Alex's Mobs artifact with production startup evidence. The independent non BFS fixture, SmartBrainLib restoration oracle, security lifecycle exits, and normal moving obstacle route are passed. The third party follow interaction and real process restart invalidation remain open. The focused fixtures prove the server lease boundary and arbitration, but they do not prove every controller family.

The earlier laptop run used the previous packaged candidate jar at SHA256 `f0d839239c7a9b0efc68a83fb9837b09b75dad3c1d05f696ba413158d7cc02ef`, joined the disposable `click3-world` dedicated server, and kept the exact Java playback stream muted. Capture `c62ebbe0-2c07-4d87-a83a-db10b4d2ed73` recorded a real right click and produced `follow.claim`, `follow.adapter`, and `follow.intent` for a persistent invulnerable `minecraft:cow`. The current candidate run used source commit `fc01cf514b57964f3681368066a8263318d0cf0d` and jar SHA256 `d0725a88e2ac7789d62d08279f1482550dbfacd941f032bd6f30e05970f7a366` on the disposable `thirdparty` server. Capture `898dc3b4-4e7c-4217-891b-39dcb4f598cb` recorded one cow claim, the normal navigation adapter, intent acceptance, arrival release, ordinary controller restoration, and repeated `arrival_latched` rejects while the cow remained at `0.5,41.0,3.5`. Capture `437810f1-8aa6-4171-a412-27059ad471cb` recorded the same sequence for `alexsmobs:grizzly_bear`; the bear remained at `0.5,41.0,6.5` after the lease. A later server restart preserved both entities and the old marker was rejected by the authorization reset GameTest. The fresh bound capture `3f90e704-9ffc-4545-a052-9bc9dbcf307c` records the exact source commit, artifact hash, server configuration, datapack fingerprint, and Java runtime after restart. These traces show that follow leases do not move or remove the animals. The disposable server, client, captures, screenshots, and copied Prism instance from that run were removed after the final evidence consumer. The unrelated personal client was preserved. Phase integration, merge, default branch verification, tag, wiki synchronization, and final artifact delivery remain blocked until the remaining gates are complete.

## Owner click retry on 2026-09-14

A fresh Java 17 dedicated server and disposable laptop client were bound to source commit `aa053bb` and the Forge jar SHA256 `038aa4c1d7a32bafcb138f24b94c402ddd596cd3f54cce737ccbee172ee0a845`. The server used port `25871`, the world `bfs2-phase-002-owner-click-20260914b`, a force loaded stone test room, and a persistent named `minecraft:cow` target. The player joined the correct private endpoint, received the issued stick, and the target was visibly centered with the stick selected. The exact client process stream was muted and read back as muted.

The bounded `follow` capture selected the cow explicitly and expired after 1200 ticks without an owner right click. It produced no claim evidence, so this attempt does not close the laptop interaction gate. The server was stopped cleanly, the disposable client and server were terminated, the temporary runtime, world, screenshots, launch log, and copied Prism instance were removed and verified absent, and the personal Prism instance and its muted stream remained intact.

## Current follow regression verification on 2026-09-14

The current phase candidate is source commit `b146dd10312366e3db1118328fcb5aff4ec49ab9` with Forge testing JAR SHA256 `69d76f0409dcd361f7618674348352c9b96e3a02ef117c79e9a217705eb1ab7a` and SHA512 `baf9f17d1542c87839d1a017e1cdb8eb951db5ba9d9b0fdc2eb5aa4c1a24422f9d8fb49dc51d67e0cbe7e0e17954681d972dde1d5268c6f1cfc8f82e0f9e24f1`. The follow route now submits direct mob navigation together with the SmartBrainLib `WALK_TARGET` memory, and issued marker placement synchronizes the selected hotbar slot to the client.

The focused `bfsfollow` GameTest run passed all 19 required tests after these changes. The navigation fixtures proved reduced distance for a normal cow, a vanilla slime, a SmartBrainLib Harbor Seal, and a Great White Shark. The third party fixture now also asserts that a pinned Alex's Mobs target reduces its distance to the owner before releasing the lease, but it requires execution in the pinned external runtime because the development classpath cannot load Citadel's production mixin refmap. The disposable run directory was removed and verified absent. `:forge:compileJava`, `:forge:compileTestJava`, the focused GameTest run, parser tests, `:forge:build`, jar integrity, and `git diff --check` also passed. The testing JAR was rebuilt after this regression fixture and its hashes are recorded above.

A fresh Java 17 dedicated server and silent NVIDIA laptop client used the current JAR and the pinned Alex's Mobs and Citadel dependencies. The client joined the exact private endpoint, the Java playback stream was muted and read back as muted, the issued stick was visibly selected, and a persistent invulnerable cow was centered in the crosshair. Two bounded follow captures, 600 and 1200 ticks, expired with one header and one terminal record each and no claim. The server status remained `issued=true, following=false, target=none`. No client input was synthesized. The owned server, client, audio stream, runtime, screenshots, and copied Prism instance were stopped or removed and verified absent. This current run therefore strengthens the current-candidate and cleanup evidence but leaves the real laptop interaction gate unverified.

## External movement retry on 2026-09-14

A disposable production Forge 47.2.0 server loaded the current candidate source `b146dd10312366e3db1118328fcb5aff4ec49ab9`, JAR SHA256 `69d76f0409dcd361f7618674348352c9b96e3a02ef117c79e9a217705eb1ab7a`, GeckoLib 4.4.7, SmartBrainLib 1.14.2, Alex's Mobs 1.22.9, and Citadel 2.6.0. The server used Java 17.0.19 on `100.76.164.109:25901` and reached `Done`. A matching laptop client used Java 17.0.15, the RTX 5090 renderer, and the same pinned artifacts. Its exact Java playback stream was node 110, correlated to the owned Minecraft process, and read back as muted.

The disposable world was prepared with a persistent named `alexsmobs:grizzly_bear` on a stone lane and a newly issued follow stick. The first setup allowed the bear to kill the survival player before interaction. The player was then placed in creative mode and the fixture was recreated, but the client remained on the Minecraft death screen and no owner respawn or right click arrived during the bounded observation. No claim, movement, or release record was accepted from this attempt. The server, client, exact audio stream, world, runtime, copied Prism instance, and screenshots were stopped or removed and verified absent. This proves current production startup and cleanup only; the pinned external movement and residual laptop input gates remain open.

## Forge interaction registration hardening on 2026-09-14

The follow manager now registers its two entity interaction handlers as static Forge subscribers with lowest priority and canceled event delivery. This keeps the marked stick callback on the server event bus even when a target's ordinary interaction handler has already canceled the event. Source commit `1b6140bbd8384f48ee7c14f2d456a9b4c9791634` adds a GameTest that posts a marked entity interaction through `MinecraftForge.EVENT_BUS` and verifies both claim and movement.

The focused `bfsfollow` GameTest run passed all 20 required tests, including the new event bus path and the vanilla slime, cow, SmartBrainLib seal, and Great White Shark movement fixtures. `./gradlew :forge:test --no-daemon` and `./gradlew :forge:build --no-daemon` passed. The rebuilt Forge jar passed `unzip -tqq` with these hashes:

```text
sha256 b20b0cc4b4703a01708445414d3f129c110f94440df430d9f863e524fc2666fd
sha512 5d50638ec9837ba5a4babc2dddf854c6c220467353a3303e8bf502e002714ad410e46c97b69f7aeebd7ba24e97bb3bc972ce1b1d5fbdb4c7a5d42cbff5d7cf29
```

The four block arrival radius remains intentional. When a claimed mob reaches that radius, the lease releases and ordinary AI resumes. A mob can still look at the owner after that release, but it will not keep following until a fresh valid selection after the arrival latch clears. The laptop gate remains open because the remote Xwayland input bridge dropped right click packets, including vanilla block placement, so no real click claim was accepted from that run.

## Current candidate click retry on 2026-09-14

A disposable production Forge 47.2.0 server and matching silent laptop client used source commit `5d2e27d4c2d1a450802dd9d1bf42afa9345994ad` and the current Forge JAR SHA256 `b20b0cc4b4703a01708445414d3f129c110f94440df430d9f863e524fc2666fd`. The JAR SHA512 is `5d50638ec9837ba5a4babc2dddf854c6c220467353a3303e8bf502e002714ad410e46c97b69f7aeebd7ba24e97bb3bc972ce1b1d5fbdb4c7a5d42cbff5d7cf29`. The server used Java 17.0.19 on `100.76.164.109:25911` in world `bfs2-phase-002-click-current`. The client used Java 17.0.15 and the NVIDIA GeForce RTX 5090 Laptop GPU. Its exact Java playback stream was node 110 and read back as `Volume: 1.00 [MUTED]`.

The server reached `Done`, the client joined the exact endpoint, and the issued marker was selected with the current owner and issue identifier. Two bounded follow captures, `f67ad86a-0f25-4bd3-98f6-0bc5255f1bb9` and `01d432b2-9540-472c-bb88-5554b06080fd`, expired without a claim. Camera movement worked after the owned window was focused, but the remote UInput and Xwayland right button path did not deliver a Forge entity interaction event. No claim, movement, or release result was accepted from this attempt. The server, client, audio stream, runtime, screenshots, and copied Prism instance were stopped or removed and verified absent, and the personal instance was preserved.

This retry binds the current candidate and cleanup state only. It does not close the real laptop right click or moving follow gate.

## Aquatic follow navigation handoff fix on 2026-09-14

The follow route now overrides the entity overload of `PitchSwimmingNavigation.moveTo`. Vanilla `PathNavigation` builds an entity path directly through that overload, so the earlier implementation never captured the exact owner destination for a shark or other pitch driven swimmer when the pathfinder rejected or reduced the path. The swimmer could turn toward the owner while receiving no finite movement destination. Explicit navigation stop now also clears the captured destination and its owned pitch route, while preserving ordinary controller scheduling.

Source commit `c5875bba822e507177b3429f2dcb675279a6f107` was verified with the Forge JAR SHA256 `c2103661dd43797ce19a521e8c69157c349a3766a3c4b262ee50d84a162ab642` and SHA512 `f0dc7a7dd4154969d695d77fe426fa780e7976ac59de3e6a9029ecb93a2261c4f1d47cbf584d730e91aa7e5daee2b0000ad8cb685b15286aeaaa99e5dbeee0ea`. The focused `bfsfollow` GameTest run passed all 21 required tests, including the new shark destination capture and release regression, the event bus path, vanilla cow and slime, SmartBrainLib Harbor Seal, Great White Shark movement, lifecycle, security, arbitration, and controller family cases. The runtime `/tmp/bfsm-p002-release-fix-20260914e` was stopped, removed, and verified absent. Full `:forge:test` and `:forge:build` also passed, and the rebuilt archive passed `unzip -tqq`.

This fixes the server side aquatic route handoff that can present as a mob looking at the owner without moving. The real laptop right click gate remains separate and unverified because the remote input bridge still dropped the right button event.
